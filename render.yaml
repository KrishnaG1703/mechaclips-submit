services:
  - type: web
    name: mechaclips-submit
    runtime: node
    plan: starter
    buildCommand: npm install
    startCommand: node server.js
    disk:
      name: clips
      mountPath: /opt/render/project/src/uploads
      sizeGB: 10
